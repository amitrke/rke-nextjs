export type ShowImageParams = {
    id?: string;
}

const ShowImage = (params: ShowImageParams) => {
    return (
        <div>
            Display an Image
        </div>
    )
}

export default ShowImage;